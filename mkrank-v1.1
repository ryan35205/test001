#!/bin/bash
rank=0
/cm/shared/apps/pbspro/11.2.0.113417/bin/qstat -f $PBS_JOBID |tr -d '\n'| tr -d '\t'|grep -oP 'compute[0-4]{2}.cm.cluster/[0-7]' | sed 's/.cm.cluster\// /g' |while read node core
do echo "rank $((rank ++))=$node slot=$core" >> /home/xiss/Testing/LS_Dyna/LS_Bench/rankfile
done